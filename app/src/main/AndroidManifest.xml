<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miclaseholaworld">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_new_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_new_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiClaseHolaWorld">
        <activity
            android:name=".SecondActivity"
            android:label="@string/activity_second"
            android:exported="false" />
        <activity
            android:name=".CategoriaActivity"
            android:exported="false"
            android:label="@string/title_activity_categoria"
            android:theme="@style/Theme.MiClaseHolaWorld.NoActionBar" />
        <activity
            android:name=".ProductActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:label="@string/activity_main"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>